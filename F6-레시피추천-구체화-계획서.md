# F6 재료 기반 레시피 추천 — 구체화 계획서

> 원본: `saeujin/Claude-Code-Camp` main 브랜치 `기능명세서.md` — 식단앱 기능 명세서 F6
> 단계: 웹 구현 착수 직전 구체화 (명세 → 데이터 구조 · 화면 · 로직 · 수락 기준)
> 관련 문서: [F3 운동 기록 및 소모 칼로리 반영 구체화 계획서](./F3-운동기록-구체화-계획서.md)

---

## 0. 이 문서가 하는 일

기능명세서는 "무엇을 만들 것인가"까지만 정의한다. 이 문서는 F6을 코드로 옮기기 위해 다음을 확정한다.

1. 레시피 · 재료 데이터의 정확한 형태 (필드명 · 타입 · 단위)
2. 매칭률 계산과 정렬을 단계별로 분해한 알고리즘과 점수식
3. 화면 구성과 상호작용 순서
4. 예외 상황별 UI 동작과 **실제 노출 문구**
5. F4·F5가 아직 없어도 F6을 완성할 수 있게 하는 인터페이스 계약
6. 구현 완료를 판정할 수락 기준

원본 명세의 예시(두부 계란찜 100% · 닭가슴살 볶음 75% · 부족 재료 파프리카)는 그대로 **회귀 테스트 케이스**로 승격시켰다.

**F6의 위치** — F6은 F4가 관리하는 보유 재료를 읽어 레시피를 정렬하는 기능이다. 재료를 버리지 않게 하는 것이 목적이므로, **유통기한 임박 재료를 쓰는 레시피를 위로 올리는 것이 이 기능의 존재 이유**에 가깝다. 잔여 칼로리 조건은 선택 입력이며, 이를 켜면 F3이 계산한 오늘 목표에 간접적으로 종속된다.

**명세에 없지만 이 문서가 추가한 것** — 재료명 동의어 사전(2.4). 사용자가 F4에 "달걀"로 등록하고 레시피가 "계란"으로 적혀 있으면 매칭률이 무의미해진다. 명세는 이 문제를 다루지 않았다.

---

## 1. 선행 결정 사항

명세서 5장 「미결정 사항」 중 F6에 걸리는 항목을 아래와 같이 정한다. 이 결정이 없으면 구현을 시작할 수 없다.

| # | 항목 | 결정 | 근거 |
|---|---|---|---|
| 5 | 레시피 데이터 출처 | **직접 구축 정적 JSON 30종(시드).** 사용자 등록 · 외부 API는 Phase 2 | 매칭 알고리즘 검증에 30종이면 충분하고, 재료명 표기를 우리가 통제할 수 있어 정규화 부담이 줄어든다 |
| 1 | 사진 재료 인식 방식 (F4) | **MVP 미탑재.** F4는 수동 입력만 지원 | 명세도 "초기 버전을 수동 입력만으로 출시해도 F1~F7 전부 동작한다"고 명시. F6은 인식 방식과 무관하게 보유 재료 목록만 읽는다 |
| 6 | 회원 인증 | **비로그인 · 브라우저 로컬 저장(localStorage)** | MVP 속도 우선. 기기 간 동기화 미지원은 알려진 한계로 수용 |

> #1은 F4의 결정이지만 F6의 입력 품질에 직결되므로 함께 적었다. #6은 사용자 확인이 필요한 판단이다. 11장에 다시 정리했다.

---

## 2. 데이터 모델

### 2.1 레시피 (정적 데이터, `data/recipes.json`)

```
Recipe {
  id          : string
  name        : string           // '두부 계란찜'
  kcal        : number           // 1인분 기준
  servings    : number           // 1
  ingredients : RecipeIngredient[]
  steps       : string[]         // 조리법
  tags        : string[]         // ['한식','국찌개','고단백']
}

RecipeIngredient {
  name      : string    // '두부'
  qty       : number    // 0.5
  unit      : string    // '모'
  isStaple  : boolean   // true면 매칭률 계산에서 제외
}
```

### 2.2 보유 재료 — F4가 소유. F6은 읽기만 한다

```
Ingredient {
  id, name, qty, unit, purchasedAt, expiresAt?
}
```

`expiresAt`이 있고 오늘부터 3일 이내면 **임박**으로 판정한다 (명세 F4 기준).

### 2.3 기본 양념 화이트리스트 (`data/staples.json`)

명세: "조미료·기본 양념은 항상 보유한 것으로 간주해 매칭률 계산에서 제외한다."

```
소금, 후추, 식용유, 간장, 설탕, 참기름, 다진마늘, 고춧가루,
고추장, 된장, 식초, 물, 맛술, 올리고당, 케첩, 마요네즈
```

레시피 JSON을 우리가 직접 만들므로 `isStaple` 플래그를 **데이터에 미리 박아둔다.** 런타임 문자열 비교에 의존하지 않는 편이 안전하다. 화이트리스트는 데이터 작성 시 검수용 기준으로 쓰고, 사용자 등록 레시피를 허용하는 Phase 2에서 런타임 판정에 사용한다.

### 2.4 재료명 정규화 (`data/synonyms.json`)

사용자가 F4에 입력한 자유 텍스트와 레시피 재료명을 맞추기 위해 필요하다.

```
normalize(name):
  1. 공백 · 특수문자 제거, 소문자화
  2. SYNONYMS 사전 조회 → 대표어 반환

SYNONYMS (시드):
  계란   ← 달걀, 닭알
  대파   ← 파, 쪽파
  닭가슴살 ← 닭 가슴살, 치킨브레스트
  돼지고기 ← 삼겹살, 목살, 앞다리살   ← 부위 → 상위 재료 매핑
  양파   ← 흰양파
  두부   ← 부침두부, 찌개두부, 연두부
```

> **부위 → 상위 재료 매핑은 신중하게 쓴다.** "삼겹살 = 돼지고기"는 대체로 맞지만 "연두부 = 두부"는 요리에 따라 틀린다(부침에 연두부는 부적합). 시드 사전은 명백한 동의어만 넣고, 부위 매핑은 육류에 한정한다.

---

## 3. 매칭 · 정렬 알고리즘

### 3.1 단계별 정의

순수 함수로 분리한다 (`src/domain/recipe.js`).

```
입력: 보유 재료 목록, (선택) 지정 재료, (선택) 잔여 칼로리 + 반영 여부

1) 보유 재료 정규화 → Set  ownedSet

2) 각 레시피에 대해:
     essentials = ingredients.filter(i => !i.isStaple)
     matched    = essentials.filter(i => ownedSet.has(normalize(i.name)))
     missing    = essentials − matched
     matchRate  = matched.length / essentials.length       // 0.0 ~ 1.0

3) 필터
     - "사용하고 싶은 재료"를 지정했으면, 그 재료를 essentials에 포함하지 않는
       레시피는 제외
     - missing.length > 2 인 레시피는 제외        ← 명세: 부족 1~2개까지만 노출

4) 점수
     score = matchRate × 100
           + expiryBonus        // 임박(3일 이내) 재료를 matched에 포함 → 1개당 +10, 최대 +20
           + caloriePenalty     // 잔여 칼로리 반영 ON일 때만
                                //   kcal ≤ remaining × 1.15  →   0
                                //   초과                     → −30  (후순위로 밀되 숨기지 않음)

5) 정렬  score desc → missing.length asc → kcal asc
```

### 3.2 함수 시그니처

| 함수 | 시그니처 | 비고 |
|---|---|---|
| `normalize` | `(name) → string` | 공백/특수문자 제거 + 동의어 사전 |
| `matchRecipe` | `(recipe, ownedSet) → { matched, missing, matchRate }` | staple 제외 후 계산 |
| `scoreRecipe` | `(matchResult, { expiringSet, remaining, useCalorieFilter }) → number` | 3.1의 4단계 |
| `rankRecipes` | `(recipes, ingredients, options) → RankedRecipe[]` | 필터 + 점수 + 정렬 일괄 |

### 3.3 검증 케이스 — 명세 S6 재현

보유: 양파, 계란, 두부(유통기한 3일 뒤 = **임박**), 대파, 닭가슴살

| 레시피 | 필수 재료 (staple 제외) | matched | matchRate | 임박 보너스 | score |
|---|---|---|---|---|---|
| 두부 계란찜 | 두부, 계란, 대파 (소금은 staple) | 3 | **100%** | 두부 → +10 | **110** |
| 닭가슴살 볶음 | 닭가슴살, 양파, 대파, 파프리카 | 3 | **75%** | 없음 | **75** |

정렬 결과: 두부 계란찜(100%, 210 kcal) → 닭가슴살 볶음(75%, 부족: 파프리카). 명세 S6과 일치한다.

### 3.4 검증 케이스 — 정규화

F4에 **"달걀"** 로 등록한 상태에서 "계란"을 쓰는 레시피가 매칭되어야 한다.
`normalize('달걀') === normalize('계란') === '계란'`

---

## 4. 인터페이스 계약

F6을 먼저 만들면 아직 없는 기능의 데이터를 참조해야 한다. **셀렉터 함수를 먼저 정의하고, 미구현 기능은 스텁으로 채운다.** 이렇게 하면 F4·F5 없이도 F6을 완성하고 테스트할 수 있다.

### 4.1 F6이 소비하는 것

| 셀렉터 | 소유 | 용도 | 스텁 반환값 |
|---|---|---|---|
| `getIngredients()` | F4 | 매칭 입력, 임박 판정 | 시드 5종 (양파 · 계란 · 두부(임박) · 대파 · 닭가슴살) |
| `getTodayTarget(date)` | **F3** | 잔여 칼로리 계산의 기준선 | `1892` |
| `getIntakeTotal(date)` | F2 | 잔여 칼로리 계산 | `1450` |
| `getProfile()` | F1 | 잔여 칼로리 토글 활성 여부 판단 | 프로필 완료 상태 |

잔여 칼로리 = `getTodayTarget(오늘) − getIntakeTotal(오늘)` → 스텁 기준 **442 kcal** (명세 S6과 동일).

> `getTodayTarget`은 [F3 문서 4.2](./F3-운동기록-구체화-계획서.md)에서 시그니처를 고정한 함수다. F6은 이 함수만 호출하고 운동 로직은 알지 않는다.

### 4.2 F6이 제공하는 것

```
getRecipeRecommendations({ pinnedIngredient?, useCalorieFilter? })
  → RankedRecipe[]
```

F7의 「F5 추천 바로가기」와 나란히 놓일 진입점 외에 다른 기능이 F6을 소비하지는 않는다.

---

## 5. 화면 구성

```
┌─ ① 조건 영역 ──────────────────────────────┐
│  보유 재료 (5)                              │
│  [양파] [계란] [두부 ⏰] [대파] [닭가슴살]    │  ← 칩. 탭하면 "사용하고 싶은 재료"로 지정
│                                            │
│  ☐ 오늘 남은 칼로리(442 kcal) 안에서 찾기    │  ← 토글. F3·F2에서 값 조회
│                                            │
│  선택한 재료: 두부  [해제]                   │  ← 지정했을 때만
└────────────────────────────────────────────┘

┌─ ② 결과 리스트 ────────────────────────────┐
│  ┌──────────────────────────────────────┐  │
│  │ 두부 계란찜             [100%] 210kcal│  │
│  │ ⏰ 유통기한이 임박한 두부를 써요        │  │
│  │ 보유: 두부 · 계란 · 대파               │  │
│  └──────────────────────────────────────┘  │
│  ┌──────────────────────────────────────┐  │
│  │ 닭가슴살 볶음            [75%] 320kcal│  │
│  │ 보유: 닭가슴살 · 양파 · 대파            │  │
│  │ 부족: 파프리카                         │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘

┌─ ③ 상세 (카드 탭) ─────────────────────────┐
│  두부 계란찜  ·  210 kcal  ·  1인분          │
│  재료표 (보유 / 부족 / 양념 구분)             │
│  조리법 1..N                                │
│  ─────────────────────────────────────────  │
│  "다 쓴 재료는 냉장고에서 직접 지워주세요"     │
│  [ 냉장고 관리로 이동 ]  [ 이 요리 식단에 기록 ]│
└────────────────────────────────────────────┘
```

### 5.1 컴포넌트 분해

| 컴포넌트 | 책임 |
|---|---|
| `IngredientChipBar` | 보유 재료 칩, 임박 표시, 지정 상태 관리 |
| `CalorieFilterToggle` | 잔여 칼로리 반영 on/off. 잔여값 표시 |
| `RecipeCard` | 이름 · 매칭률 배지 · kcal · 보유/부족 재료 · 임박 사유 |
| `RecipeDetail` | 재료표 + 조리법 + 재료 차감 안내 |
| `EmptyIngredientState` | 보유 재료 0건 → F4 유도 |

**매칭률 배지** — 100% / 75~99% / 50~74% / 그 이하를 시각적으로 구분하되, **색만으로 정보를 전달하지 않도록 숫자를 항상 함께 표기한다.**

---

## 6. 상호작용 플로우

### 6.1 기본 플로우

1. 진입 시 `getIngredients()` 조회 → 0건이면 빈 상태로 분기
2. `rankRecipes`를 조건 없이 1회 실행 → 결과 리스트 렌더
3. 재료 칩을 탭하면 그 재료를 `pinnedIngredient`로 지정 → 즉시 재정렬 (재조회 없음, 로컬 데이터)
4. 잔여 칼로리 토글을 켜면 `caloriePenalty`를 반영해 즉시 재정렬
5. 카드 탭 → 상세. 재료표는 **보유 / 부족 / 양념** 3구간으로 나눠 보여준다

### 6.2 상세에서 나가는 두 경로

- `냉장고 관리로 이동` → F4. 실제로 쓴 재료를 사용자가 직접 지운다
- `이 요리 식단에 기록` → F2에 레시피명과 kcal을 프리필한 상태로 이동

두 버튼 모두 F6이 스스로 데이터를 바꾸지 않는다는 점이 중요하다 (7장 마지막 항목).

---

## 7. 예외 처리 사양

| 조건 | UI 동작 | 노출 문구 |
|---|---|---|
| 보유 재료 0건 | 결과 영역 대신 빈 상태 | "냉장고가 비어 있어요. 재료를 등록하면 만들 수 있는 요리를 찾아드려요. [냉장고 채우기]" |
| `missing ≤ 2` 조건 통과 레시피 0건 | 빈 상태 + 완화 안내 | "지금 재료로 만들 수 있는 요리를 못 찾았어요. 재료를 몇 개 더 등록해보세요." |
| 매칭률 100% 없음 | 부족 1~2개 레시피를 그대로 노출하고 부족분을 명시 | 카드에 "부족: 파프리카" |
| 잔여 칼로리 토글 ON + 잔여 음수 | 토글은 켜지되 기준을 저칼로리로 전환 | "오늘 목표를 넘었어요. 가벼운 요리부터 보여드려요." (F5 문구와 톤 통일) |
| 잔여 칼로리 토글 ON + F1 미완료 | 토글 비활성 | "잔여 칼로리를 쓰려면 프로필 설정이 필요해요." |
| 지정 재료를 쓰는 레시피 0건 | 지정 해제 제안 | "'파프리카'를 쓰는 요리가 없어요. [지정 해제]" |
| 레시피 조회 후 (상시) | 상세 하단 고정 | "레시피를 봤다고 재료가 줄지 않아요. 실제로 쓴 재료는 냉장고에서 지워주세요." |

> **재료 자동 차감을 하지 않는 것은 버그가 아니라 결정이다** (명세: 실제 사용량을 앱이 알 수 없음). 구현 중 "차감이 안 된다"는 지적이 나오면 이 문구로 답한다.

---

## 8. 파일 구조

F6이 만들거나 건드리는 파일만 표기했다. `state/` 두 파일은 F3과 공유한다.

```
data/
  recipes.json            # 레시피 시드 30종 (isStaple 표기 포함)
  synonyms.json           # 재료명 동의어 사전
  staples.json            # 기본 양념 화이트리스트 (데이터 검수 기준)

src/
  domain/
    recipe.js             # normalize, matchRecipe, scoreRecipe, rankRecipes
  state/
    store.js              # localStorage 읽기/쓰기            ← F3과 공유
    selectors.js          # 4장의 셀렉터 계약 (미구현 기능은 스텁)  ← F3과 공유
  pages/
    recipes.html/js
  components/
    IngredientChipBar / CalorieFilterToggle / RecipeCard /
    RecipeDetail / EmptyIngredientState

test/
  recipe.test.js          # 3.3 · 3.4의 검증 케이스
```

`domain/recipe.js`는 DOM·localStorage를 일절 참조하지 않는다. 매칭 순위가 이 기능의 신뢰도 전부이므로, UI 없이 테스트 가능해야 한다.

---

## 9. 구현 순서

| 순서 | 작업 | 완료 판정 |
|---|---|---|
| 1 | `data/recipes.json` 시드 작성 (최소 10종, 목표 30종) | 두부계란찜 · 닭가슴살볶음 포함, `isStaple` 표기 완료 |
| 2 | `data/synonyms.json` · `staples.json` | 2.3 · 2.4의 시드 항목 반영 |
| 3 | `domain/recipe.js` + 단위 테스트 | 3.3 순위·점수 일치, 3.4 정규화 통과 |
| 4 | `state/selectors.js` 스텁 (4.1) | F4·F2 없이 잔여 칼로리 442가 표시됨 |
| 5 | 조건 영역 → 결과 리스트 → 상세 | 5장 구성 완성, 6장 플로우 동작 |
| 6 | 예외 처리 7건 | 7장 표 전항 확인 |
| 7 | 레시피 30종까지 확충 | 매칭률 분포가 100%에 몰리지 않는지 확인 |

3번(순수 로직)을 화면보다 먼저 만든다. 순위가 틀린 채로 화면을 예쁘게 만들면 나중에 뜯어야 한다.

**F3과의 순서 관계** — 4.1의 `getTodayTarget` 스텁만 있으면 F6은 F3보다 먼저 만들어도 된다. 두 기능은 `state/` 두 파일만 공유하므로 병렬 작업이 가능하다.

---

## 10. 수락 기준

- [ ] 보유 5종(양파 · 계란 · 두부 · 대파 · 닭가슴살)에서 **두부 계란찜이 100%로 1위**, 닭가슴살 볶음이 75%로 2위다
- [ ] 두부 계란찜 카드에 유통기한 임박 재료 사용 사유가 표시된다
- [ ] 닭가슴살 볶음 카드에 `부족: 파프리카`가 표시된다
- [ ] 레시피의 소금 · 식용유는 매칭률 계산에 포함되지 않는다 (분모에서 제외)
- [ ] F4에 "달걀"로 등록해도 "계란"을 쓰는 레시피가 매칭된다
- [ ] 부족 재료가 3개 이상인 레시피는 목록에 나오지 않는다
- [ ] 잔여 칼로리 토글을 켜면 초과 레시피가 **숨겨지지 않고 후순위로** 내려간다
- [ ] 잔여 칼로리가 음수일 때 토글을 켜면 저칼로리 요리부터 나온다
- [ ] 레시피 상세를 봐도 냉장고 재료가 줄지 않고, 그 사실이 안내된다
- [ ] 보유 재료 0건이면 F4로 유도하는 빈 상태가 나온다
- [ ] 재료를 지정하면 그 재료를 쓰는 레시피만 남고, 0건이면 해제를 제안한다
- [ ] 매칭률이 숫자로 항상 표기된다 (색만으로 구분하지 않는다)

---

## 11. 확인이 필요한 결정

1장에서 잠정 결정했지만, 뒤집히면 작업량이 달라지는 항목.

| 항목 | 잠정 결정 | 뒤집힐 경우 영향 |
|---|---|---|
| **레시피 데이터 출처 (#5)** | 정적 JSON 30종 직접 구축 | 외부 API로 가면 재료명을 우리가 통제할 수 없어 **동의어 사전(2.4)의 규모가 크게 늘어난다.** F6에서 가장 비용이 큰 변경 |
| **사진 재료 인식 (#1)** | MVP 미탑재 (F4 수동 입력만) | F6 로직은 영향 없음. 다만 인식 오류가 섞인 재료 목록이 들어오면 매칭 품질이 떨어지므로 F4의 사용자 확인 단계가 필수다 |
| **인증 (#6)** | 비로그인 localStorage | 로그인을 넣으면 저장 계층과 셀렉터가 비동기로 바뀌어 8장 구조가 변경된다 |

미결정 항목 중 **점수식의 가중치(임박 +10/최대 +20, 칼로리 초과 −30)는 이 문서에서 처음 정한 값이다.** 명세에는 "가산점을 준다", "후순위로" 라고만 적혀 있다. 시드 30종으로 순위를 눈으로 확인한 뒤 조정할 수 있도록 상수로 분리해 둔다.

그리고 이 문서가 정하지 않은 것 하나 — **프레임워크**. 위 구조는 바닐라 JS + localStorage를 가정해 썼다. React/Vue를 쓸 경우 `domain/`과 `data/`는 그대로 유효하고 `pages/`·`components/`만 바뀐다.
