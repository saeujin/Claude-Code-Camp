# F3 운동 기록 및 소모 칼로리 반영 — 구체화 계획서

> 원본: `saeujin/Claude-Code-Camp` main 브랜치 `기능명세서.md` — 식단앱 기능 명세서 F3
> 단계: 웹 구현 착수 직전 구체화 (명세 → 데이터 구조 · 화면 · 로직 · 수락 기준)

---

## 0. 이 문서가 하는 일

기능명세서는 "무엇을 만들 것인가"까지만 정의한다. 이 문서는 F3을 코드로 옮기기 위해 다음을 확정한다.

1. 저장할 데이터의 정확한 형태 (필드명 · 타입 · 단위)
2. MET 계산과 단백질 하한 상향을 순수 함수 단위로 분해한 시그니처와 반올림 규칙
3. 화면 구성과 상호작용 순서
4. 예외 상황별 UI 동작과 **실제 노출 문구**
5. F1·F2가 아직 없어도 F3을 완성할 수 있게 하는 인터페이스 계약
6. 구현 완료를 판정할 수락 기준

원본 명세의 숫자 예시(조깅 30분 → 311 kcal, 근력운동 시 단백질 119g → 120g)는 그대로 **회귀 테스트 케이스**로 승격시켰다.

**F3의 위치** — F3은 F1이 세운 기준선(기본 목표 칼로리)에 그날의 여유분을 더하는 기능이다. F5(다음 식사 추천)와 F7(일일 대시보드)이 F3의 출력을 입력으로 받으므로, F3이 제공하는 셀렉터 3종(4장)의 시그니처를 고정하는 것이 이 문서의 가장 중요한 산출물이다.

---

## 1. 선행 결정 사항

명세서 5장 「미결정 사항」 중 F3에 걸리는 항목을 아래와 같이 정한다. 이 결정이 없으면 구현을 시작할 수 없다.

| # | 항목 | 결정 | 근거 |
|---|---|---|---|
| 3 | 운동 MET 데이터 출처 | **Compendium of Physical Activities 값을 인용해 정적 JSON으로 직접 구축.** 명세의 12종을 필수로, 확장 후보 12종을 추가해 총 24종 | 명세도 "20~30종이면 초기에 충분"이라고 판단. 외부 API 의존 제거 |
| 4 | 웨어러블 · 건강앱 연동 (입력 경로 ③) | **MVP 미탑재.** 설정 화면에 항목만 두고 비활성 처리 | 웹 앱에서 Apple Health / Samsung Health 직접 접근 불가. 명세도 "연동은 선택 사항이므로 이 결정이 늦어져도 F3는 동작한다"고 명시 |
| 8 | 운동 소모 칼로리 반영 비율 | **100% 반영 + 안내 문구.** 명세 기술 방식 유지. 반영 비율 설정 토글은 Phase 2로 미룸 | 명세의 S3 시나리오·F7 대시보드 수치가 100% 반영을 전제로 쓰여 있음. 비율을 바꾸면 예시 숫자 전체가 흔들린다 |
| 6 | 회원 인증 | **비로그인 · 브라우저 로컬 저장(localStorage)** | MVP 속도 우선. 기기 간 동기화 미지원은 알려진 한계로 수용 |

> 위 4건 중 #4·#8·#6은 사용자 확인이 필요한 판단이다. 8장에 다시 정리했다.

---

## 2. 데이터 모델

### 2.1 운동 카탈로그 (정적 데이터, `data/exercises.json`)

```
ExerciseCatalogItem {
  id        : string    // 'jog_8'
  name      : string    // '조깅 (8km/h)'
  met       : number    // 8.3
  type      : 'cardio' | 'strength'
  keywords  : string[]  // ['조깅','달리기','러닝','jogging'] — 검색용
}
```

### 2.2 운동 기록 (사용자 데이터, localStorage)

```
ExerciseLog {
  id          : string           // uuid
  date        : string           // 'YYYY-MM-DD'  (기본값: 오늘)
  time        : string           // 'HH:mm'       (기본값: 현재)
  source      : 'catalog' | 'manual'
  catalogId   : string | null    // source='catalog'일 때만
  name        : string           // 표시용 이름 (manual이면 사용자 입력)
  durationMin : number | null    // source='manual'이면 null 허용
  calories    : number           // 정수. catalog면 계산값, manual이면 입력값
  type        : 'cardio' | 'strength'   // manual일 때 사용자가 선택
  weightSnapshot : number        // 계산에 쓴 체중(kg). 이후 체중 변경 시 과거 기록 보존용
  createdAt   : number
}
```

> `weightSnapshot`은 명세에 없지만 필요하다. 체중을 갱신하면 F1의 BMR·TDEE는 재계산되지만 **과거 운동 기록의 소모 칼로리는 그때의 체중으로 계산된 값**이어야 한다. 이 필드가 없으면 어제 기록이 오늘 체중으로 조용히 바뀐다.

### 2.3 파생 상태 (저장하지 않고 매번 계산)

```
DailyExerciseState {
  exerciseTotal    : number   // 그날 소모 칼로리 합계
  hasStrength      : boolean  // 근력 유형 기록 1건 이상
  todayTarget      : number   // 기본 목표 + exerciseTotal
  proteinFloorG    : number   // 체중 × (hasStrength ? 1.6 : 1.2)
}
```

---

## 3. 계산 로직

### 3.1 순수 함수 명세

전부 순수 함수로 분리한다 (`src/domain/exercise.js`). UI 없이 단위 테스트가 가능해야 한다.

| 함수 | 시그니처 | 규칙 |
|---|---|---|
| `calcExerciseCalories` | `({ met, weightKg, durationMin }) → number` | `met × weightKg × (durationMin / 60)`, **결과만 반올림**(중간 반올림 금지) |
| `sumExerciseCalories` | `(logs, date) → number` | 해당 날짜 `calories` 합. 각 값이 이미 정수이므로 추가 반올림 없음 |
| `hasStrengthTraining` | `(logs, date) → boolean` | `type === 'strength'` 1건 이상 |
| `calcTodayTarget` | `(baseTarget, exerciseTotal) → number` | 단순 덧셈. 상한 없음 |
| `calcProteinFloorG` | `(weightKg, hasStrength) → number` | `weightKg × (hasStrength ? 1.6 : 1.2)`, 반올림 |
| `applyProteinFloor` | `(macros, floorG) → macros` | 단백질이 하한 미달이면 하한값으로 올리고, **늘어난 그램 × 4 kcal를 탄수화물에서 차감**(÷4 하여 그램 환산) |

### 3.2 검증 케이스 A — 명세 S3 재현

전제: 남 30세 175cm 75kg, 사무직(1.2), 목표 70kg/12주 → 기본 목표 1,581 kcal / 탄158 · 단119 · 지53

```
조깅 30분:  8.3 × 75 × (30/60) = 311.25 → 311 kcal
오늘 목표:  1,581 + 311 = 1,892 kcal
```

### 3.3 검증 케이스 B — 근력운동 시 단백질 하한 상향

```
웨이트(보통) 40분: 3.5 × 75 × (40/60) = 175.0 → 175 kcal
hasStrength = true → 단백질 하한 75 × 1.6 = 120 g
계산값 119 g < 120 g  →  단백질 120 g
차감분 (120 − 119) × 4 = 4 kcal → 탄수 4 ÷ 4 = 1 g 차감
결과: 탄수 157 g / 단백질 120 g / 지방 53 g
```

이 두 케이스는 명세 F1의 「계산 예시 ㉮」 하단 주석과 정확히 일치한다.

### 3.4 MET 카탈로그

**필수 12종 — 명세에 명시된 값을 그대로 사용 (변경 금지)**

| id | 이름 | MET | 유형 | 75kg·30분 |
|---|---|---|---|---|
| `walk_48` | 걷기 (평지 4.8km/h) | 3.5 | cardio | 131 kcal |
| `walk_64` | 빠르게 걷기 (6.4km/h) | 5.0 | cardio | 188 kcal |
| `jog_8` | 조깅 (8km/h) | 8.3 | cardio | 311 kcal |
| `run_97` | 달리기 (9.7km/h) | 9.8 | cardio | 368 kcal |
| `cycle_flat` | 자전거 (평지) | 7.5 | cardio | 281 kcal |
| `swim_free` | 수영 (자유형) | 5.8 | cardio | 218 kcal |
| `hiking` | 등산 | 6.0 | cardio | 225 kcal |
| `jumprope` | 줄넘기 | 11.8 | cardio | 442 kcal |
| `yoga` | 요가 | 2.5 | cardio | 94 kcal |
| `weight_mod` | 웨이트 (보통 강도) | 3.5 | strength | 131 kcal |
| `weight_high` | 웨이트 (고강도) | 6.0 | strength | 225 kcal |
| `bodyweight` | 맨몸운동 | 3.8 | strength | 142 kcal |

**확장 후보 12종** — 값은 Compendium 기준으로 채우되, **구현 전 출처 확인이 필요한 값**으로 표시한다. 검증이 늦어지면 이 12종 없이 출시해도 무방하다 (경로 ② 직접 입력으로 커버).

배드민턴 / 테니스 / 축구 / 농구 / 배구 / 필라테스 / 스피닝 / 로잉머신 / 계단 오르기 / 스트레칭 / 복싱 / 크로스핏

---

## 4. 인터페이스 계약

F3을 먼저 만들면 아직 없는 기능의 데이터를 참조해야 한다. **셀렉터 함수를 먼저 정의하고, 미구현 기능은 스텁으로 채운다.** 이렇게 하면 F1·F2 없이도 F3을 완성하고 테스트할 수 있다.

### 4.1 F3이 소비하는 것

| 셀렉터 | 소유 | 용도 | 스텁 반환값 |
|---|---|---|---|
| `getProfile()` | F1 | 체중(MET 계산), 목표 유형(안내 문구 분기) | 남 30세 175cm 75kg 사무직 다이어트 |
| `getBaseTargetCalories()` | F1 | 요약 카드의 「기본 목표」 | `1581` |
| `getTargetMacros()` | F1 | 단백질 하한 적용 전 목표 탄단지 | `{c:158, p:119, f:53}` |
| `getIntakeTotal(date)` | F2 | 잔여 칼로리 계산 | `0` |

### 4.2 F3이 제공하는 것 — 시그니처 고정

F5·F7이 아래 세 함수만 보고 개발될 수 있어야 한다.

```
getTodayTarget(date)      → number
                            // 기본 목표 + 그날 운동 소모 합계

getExerciseSummary(date)  → { total: number, hasStrength: boolean, logs: ExerciseLog[] }
                            // F7의 목표 구성 표시 (기본 + 운동 = 오늘)

getProteinFloorG(date)    → number
                            // F5의 단백질 가중치, F7의 탄단지 기준
```

F6도 잔여 칼로리 조건을 쓸 때 `getTodayTarget`을 간접적으로 참조한다.

---

## 5. 화면 구성

운동 기록 페이지 하나로 구성한다. 위에서 아래로 3개 블록.

```
┌─ ① 오늘 목표 요약 카드 ────────────────────┐
│  오늘 목표  1,892 kcal                     │
│  기본 목표 1,581  +  운동 311              │  ← 분해 표시 (F7과 동일 포맷)
│  [ +311 kcal 운동으로 추가됨 ]              │  ← 기록 있을 때만
│  단백질 목표 120 g  ↑ 근력운동 반영          │  ← hasStrength일 때만
└────────────────────────────────────────────┘

┌─ ② 운동 추가 ──────────────────────────────┐
│  [ 운동 선택 ]  [ 직접 입력 ]                │  ← 탭
│                                            │
│  (운동 선택 탭)                              │
│   검색창: "조깅"                             │
│   → 결과 리스트 (이름 · MET · 유형 배지)      │
│   선택: 조깅 (8km/h)                        │
│   시간: [ 30 ] 분                           │
│   ▸ 예상 소모  311 kcal          ← 실시간   │
│   시각: [ 07:30 ]   날짜: [ 2026-07-29 ]    │
│   [ 기록하기 ]                              │
│                                            │
│  (직접 입력 탭)                              │
│   운동 이름: [        ]                      │
│   소모 칼로리: [    ] kcal                   │
│   유형: ( ) 유산소  ( ) 근력   ← 필수        │
│   시각 / 날짜 / [ 기록하기 ]                 │
└────────────────────────────────────────────┘

┌─ ③ 오늘의 운동 ────────────────────────────┐
│  07:30  조깅 (8km/h)  30분  311 kcal  유산소 │ [수정][삭제]
│  19:00  웨이트 (보통)  40분  175 kcal  근력   │ [수정][삭제]
│  ─────────────────────────────────────────  │
│  합계  486 kcal                             │
└────────────────────────────────────────────┘
```

### 5.1 컴포넌트 분해

| 컴포넌트 | 책임 |
|---|---|
| `TodayTargetCard` | 기본 목표 · 운동 소모 · 오늘 목표 분해 표시, 단백질 하한 상향 배지 |
| `ExerciseSearchInput` | 카탈로그 `keywords` 부분 일치 검색 |
| `ExerciseCatalogForm` | 선택 + 시간 → 실시간 소모 칼로리 미리보기 |
| `ExerciseManualForm` | 이름 · kcal · 유형 직접 입력 |
| `ExerciseLogList` | 당일 기록 목록 + 수정/삭제 |
| `EstimateNotice` | 추정치 안내 문구 (다이어트 목표에서 강조) |

---

## 6. 상호작용 플로우

### 6.1 플로우 A — 카탈로그 경로로 기록

1. 검색창에 입력 → `keywords` 부분 일치로 필터 (디바운스 없음, 로컬 데이터)
2. 운동 선택 → 시간 입력 필드에 포커스 이동
3. 시간을 입력하는 **동시에** `calcExerciseCalories` 결과를 미리보기로 갱신
4. `기록하기` → 로그 저장 → 위 요약 카드 즉시 갱신
5. 저장 직후 **오늘 목표 숫자가 변한 사실을 눈에 보이게** 한다 (`1,581 → 1,892` 카운트업, 300ms)
6. 그날 첫 근력 기록이면 단백질 하한 상향 배너를 노출

### 6.2 플로우 B — 직접 입력 경로

유형(유산소/근력) 선택을 **필수**로 둔다. 유형을 모르면 명세 F3 처리 ③(단백질 하한 상향)을 판단할 수 없기 때문이다. 기본값을 두지 않고 미선택 시 저장 버튼을 비활성화한다.

### 6.3 플로우 C — 수정 · 삭제

- 수정: 같은 폼을 값이 채워진 상태로 열고, 저장 시 로그를 교체 후 전량 재계산
- 삭제: 확인 모달 없이 즉시 삭제 + 5초 되돌리기 스낵바
- 어느 쪽이든 `exerciseTotal` → `todayTarget` → `잔여 칼로리` → `proteinFloorG`를 순서대로 재계산
- 삭제로 잔여 칼로리가 음수가 되면 **그대로 음수로 표시한다** (명세: 정상 동작)

---

## 7. 예외 처리 사양

| 조건 | UI 동작 | 노출 문구 |
|---|---|---|
| F1 미완료 (체중 없음) | 「운동 선택」탭 비활성, 「직접 입력」탭만 사용 가능 | "MET 자동 계산에는 체중이 필요해요. 프로필을 먼저 설정하면 운동만 골라도 칼로리가 계산돼요. [프로필 설정]" |
| 시간 미입력 또는 0 | `기록하기` 비활성 | — |
| 시간 > 600분 | 저장은 허용, 경고 배지 | "10시간이 넘어요. 시간을 다시 확인해주세요." |
| 직접 입력 kcal ≤ 0 | 저장 비활성 | — |
| 직접 입력 유형 미선택 | 저장 비활성 | "유형을 골라주세요. 근력운동은 단백질 목표에 반영돼요." |
| 하루 합계 > 기본 목표 × 100% | 목록 상단에 확인 배너 (저장은 유지) | "오늘 운동 소모가 1,581 kcal를 넘었어요. 입력을 확인해주세요." |
| 모든 저장 직후 (상시) | 요약 카드 하단 고정 문구 | "소모 칼로리는 추정치예요." |
| 위 + 목표가 '다이어트' | 위 문구를 확장 | "소모 칼로리는 추정치예요. 다이어트 중이라면 늘어난 만큼 전부 채우기보다 절반 정도만 채우는 걸 권해요." |
| `walk_*` 운동 선택 시 | 폼 하단 힌트 | "출퇴근 도보처럼 매일 하는 활동은 이미 활동 수준에 반영돼 있어요. 따로 나가서 걸은 운동만 기록해주세요." |
| 외부 연동 설정 | 설정 화면에 비활성 항목으로 표시 | "웨어러블 연동은 준비 중이에요." |

> **중복 방지 로직(명세의 외부 연동 예외)은 MVP 범위에서 제외한다.** 경로 ③이 없으면 자동 수집 기록이 존재하지 않아 겹칠 대상이 없다. Phase 2에서 경로 ③을 켤 때 함께 구현한다.

---

## 8. 파일 구조

F3이 만들거나 건드리는 파일만 표기했다. `state/selectors.js`는 F6과 공유한다.

```
data/
  exercises.json          # MET 카탈로그 24종 (필수 12 + 확장 12)

src/
  domain/
    exercise.js           # calcExerciseCalories, sumExerciseCalories, hasStrengthTraining,
                          # calcTodayTarget, calcProteinFloorG, applyProteinFloor
  state/
    store.js              # localStorage 읽기/쓰기            ← F6과 공유
    selectors.js          # 4장의 셀렉터 계약 (미구현 기능은 스텁)  ← F6과 공유
  pages/
    exercise.html/js
  components/
    TodayTargetCard / ExerciseSearchInput / ExerciseCatalogForm /
    ExerciseManualForm / ExerciseLogList / EstimateNotice

test/
  exercise.test.js        # 3.2 · 3.3의 검증 케이스
```

`domain/exercise.js`는 DOM·localStorage를 일절 참조하지 않는다. 계산의 정확성이 이 앱의 신뢰도 전부이므로, UI 없이 테스트 가능해야 한다.

---

## 9. 구현 순서

| 순서 | 작업 | 완료 판정 |
|---|---|---|
| 1 | `data/exercises.json` 필수 12종 작성 | 명세 표의 MET 값과 1:1 일치 |
| 2 | `domain/exercise.js` + 단위 테스트 | 3.2 · 3.3의 검증 케이스 통과 |
| 3 | `state/selectors.js` 스텁 (4.1) | F1·F2 없이 F3 화면이 숫자를 표시 |
| 4 | 요약 카드 → 카탈로그 폼 → 목록 → 직접 입력 탭 | 6장 플로우 A·B·C 동작 |
| 5 | 예외 처리 10건 | 7장 표 전항 확인 |
| 6 | 확장 MET 12종 값 검증 후 추가 | 출처 확인 완료 (선택) |
| 7 | 제공 셀렉터 3종 공개 (4.2) | F5·F7 담당이 스텁 없이 호출 가능 |

2번(순수 로직)을 화면보다 먼저 만든다. 숫자가 틀린 채로 화면을 예쁘게 만들면 나중에 뜯어야 한다.

---

## 10. 수락 기준

- [ ] 조깅 30분 · 체중 75kg → **311 kcal**로 표시된다
- [ ] 기록 후 요약 카드가 `기본 목표 1,581 + 운동 311 = 오늘 목표 1,892`로 분해 표시된다
- [ ] `+311 kcal` 증가분이 별도로 강조된다
- [ ] 웨이트(보통) 40분 기록 시 단백질 목표가 119g → **120g**으로 올라가고 탄수가 158g → **157g**으로 줄어든다
- [ ] 근력 기록을 삭제하면 단백질 목표가 119g으로 되돌아온다
- [ ] 기록 삭제로 잔여 칼로리가 음수가 되어도 앱이 막지 않고 음수를 표시한다
- [ ] 「직접 입력」탭에서 유형을 고르지 않으면 저장할 수 없다
- [ ] 프로필 없이도 「직접 입력」탭으로 기록할 수 있다
- [ ] 다이어트 목표 사용자에게 "절반 정도만 채우는 걸 권해요" 문구가 노출된다
- [ ] 걷기 계열 선택 시 일상 활동 중복 안내가 노출된다
- [ ] 하루 소모 합계가 기본 목표를 넘으면 확인 배너가 뜬다
- [ ] 체중을 76kg으로 바꿔도 어제 기록한 조깅은 여전히 311 kcal다
- [ ] `getTodayTarget` / `getExerciseSummary` / `getProteinFloorG`가 4.2 시그니처대로 동작한다

---

## 11. 확인이 필요한 결정

1장에서 잠정 결정했지만, 뒤집히면 작업량이 달라지는 항목.

| 항목 | 잠정 결정 | 뒤집힐 경우 영향 |
|---|---|---|
| **웨어러블 연동 (#4)** | MVP 미탑재 | 입력 경로 ③과 중복 방지 로직이 추가된다. F3 작업량 약 1.5배 |
| **운동 소모 반영 비율 (#8)** | 100% 반영 | 50% 또는 사용자 선택으로 바꾸면 명세의 예시 숫자(1,892)와 F7 대시보드 표기가 전부 달라진다. **웹 착수 전에 확정해야 하는 1순위 항목** |
| **인증 (#6)** | 비로그인 localStorage | 로그인을 넣으면 저장 계층과 셀렉터가 비동기로 바뀌어 8장 구조가 변경된다 |

그리고 이 문서가 정하지 않은 것 하나 — **프레임워크**. 위 구조는 바닐라 JS + localStorage를 가정해 썼다. React/Vue를 쓸 경우 `domain/`과 `data/`는 그대로 유효하고 `pages/`·`components/`만 바뀐다.
